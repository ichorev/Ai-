{
  "name": "chatgpt-interface",
  "version": "1.0.0",
  "description": "ChatGPT interface with access code system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "openai": "^4.28.0"
  },
  "engines": {
    "node": "18.x"
  }
}
